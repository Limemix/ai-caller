{
  "name": "ai-suite",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:sip": "npm run start --workspace @ai/sip-client",
    "dev:call2telegram": "npm run dev --workspace @ai/call2telegram"
  }
}
